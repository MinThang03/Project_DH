<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Khi được chọn (selected) -->
    <item android:drawable="@drawable/bogoc5" android:state_selected="true"/>

    <!-- Mặc định (không được chọn) -->
    <item android:drawable="@drawable/bogoc3"/>
</selector>
